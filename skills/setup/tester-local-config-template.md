Edit this value in place (or re-run `/iadc-tester:setup`); this file is gitignored and personal to
this machine — each developer points it at their own local test harness.

- **Harness path:** `<the absolute filesystem path to the extracted Appian Selenium API distribution>`
